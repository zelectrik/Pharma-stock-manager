{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "npm run typecheck && npm run build",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
